{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://sourceos.dev/schemas/bearbrowser/page-summary.schema.json",
  "title": "BearBrowser Page Summary",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "summaryId",
    "timestamp",
    "product",
    "state",
    "actor",
    "source",
    "classification",
    "summary",
    "policy"
  ],
  "properties": {
    "schemaVersion": { "type": "string", "const": "bearbrowser.page_summary.v1" },
    "summaryId": { "type": "string", "minLength": 12 },
    "timestamp": { "type": "string", "format": "date-time" },
    "product": { "type": "string", "const": "BearBrowser" },
    "state": { "type": "string", "enum": ["proposed", "reviewed", "discarded"] },
    "actor": {
      "type": "object",
      "additionalProperties": false,
      "required": ["type", "id"],
      "properties": {
        "type": { "type": "string", "enum": ["human", "agent", "system", "automation"] },
        "id": { "type": "string" },
        "displayName": { "type": "string" }
      }
    },
    "source": {
      "type": "object",
      "additionalProperties": false,
      "required": ["kind"],
      "properties": {
        "kind": { "type": "string", "enum": ["page", "tab", "note", "automation", "system"] },
        "url": { "type": "string" },
        "title": { "type": "string" },
        "label": { "type": "string" }
      }
    },
    "classification": {
      "type": "object",
      "additionalProperties": false,
      "required": ["payloadClass", "secretLikeDetected", "mutationAllowed"],
      "properties": {
        "payloadClass": { "type": "string", "enum": ["public", "metadata", "sensitive-metadata", "secret-blocked"] },
        "secretLikeDetected": { "type": "boolean" },
        "mutationAllowed": { "type": "boolean", "const": false }
      }
    },
    "summary": {
      "type": "object",
      "additionalProperties": false,
      "required": ["summaryText", "excerpt", "wordCount", "method"],
      "properties": {
        "summaryText": { "type": "string" },
        "excerpt": { "type": "string" },
        "wordCount": { "type": "integer", "minimum": 0 },
        "method": { "type": "string", "enum": ["local-extractive", "manual", "agent-proposed"] }
      }
    },
    "policy": {
      "type": "object",
      "additionalProperties": false,
      "required": ["decision", "decisionId", "mode", "reason"],
      "properties": {
        "decision": { "type": "string", "enum": ["observe", "hold", "deny"] },
        "decisionId": { "type": "string" },
        "mode": { "type": "string", "enum": ["local-default", "manual", "policyfabric"] },
        "reason": { "type": "string" }
      }
    }
  }
}
