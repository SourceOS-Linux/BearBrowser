name: BearBrowser BearHistory

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  bearhistory:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout BearBrowser
        uses: actions/checkout@v4

      - name: Validate BearHistory policy artifacts
        run: bash scripts/verify-bearhistory.sh
