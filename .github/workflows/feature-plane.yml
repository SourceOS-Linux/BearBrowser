name: Feature Plane Validation

on:
  pull_request:
    paths:
      - "schemas/**"
      - "policy/local-default-actions.yaml"
      - "agent-sidecar/**"
      - "scripts/bearbrowser-emit-event.py"
      - "scripts/bearbrowser-verify-provenance.py"
      - "scripts/bearbrowser-propose-action.py"
      - "scripts/bearbrowser-resolve-action.py"
      - "scripts/bearbrowser-verify-actions.py"
      - "scripts/bearbrowser-memory-candidate.py"
      - "scripts/bearbrowser-verify-memory.py"
      - "scripts/bearbrowser-governance-queue.py"
      - "scripts/bearbrowser-sidecar-status.py"
      - "scripts/verify-feature-plane.sh"
      - "scripts/verify-sidecar-status.sh"
      - "scripts/verify-agent-sidecar-contract.py"
      - ".github/workflows/feature-plane.yml"
  push:
    branches: [main]
    paths:
      - "schemas/**"
      - "policy/local-default-actions.yaml"
      - "agent-sidecar/**"
      - "scripts/bearbrowser-emit-event.py"
      - "scripts/bearbrowser-verify-provenance.py"
      - "scripts/bearbrowser-propose-action.py"
      - "scripts/bearbrowser-resolve-action.py"
      - "scripts/bearbrowser-verify-actions.py"
      - "scripts/bearbrowser-memory-candidate.py"
      - "scripts/bearbrowser-verify-memory.py"
      - "scripts/bearbrowser-governance-queue.py"
      - "scripts/bearbrowser-sidecar-status.py"
      - "scripts/verify-feature-plane.sh"
      - "scripts/verify-sidecar-status.sh"
      - "scripts/verify-agent-sidecar-contract.py"
      - ".github/workflows/feature-plane.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate-feature-plane:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Python syntax
        run: |
          python3 -m py_compile \
            scripts/bearbrowser-emit-event.py \
            scripts/bearbrowser-verify-provenance.py \
            scripts/bearbrowser-propose-action.py \
            scripts/bearbrowser-resolve-action.py \
            scripts/bearbrowser-verify-actions.py \
            scripts/bearbrowser-memory-candidate.py \
            scripts/bearbrowser-verify-memory.py \
            scripts/bearbrowser-governance-queue.py \
            scripts/bearbrowser-sidecar-status.py \
            scripts/verify-agent-sidecar-contract.py

      - name: Shell syntax
        run: |
          bash -n scripts/verify-feature-plane.sh
          bash -n scripts/verify-sidecar-status.sh

      - name: Validate feature plane
        run: |
          bash scripts/verify-feature-plane.sh

      - name: Validate sidecar status renderer
        run: |
          bash scripts/verify-sidecar-status.sh

      - name: Validate agent sidecar contract
        run: |
          python3 scripts/verify-agent-sidecar-contract.py
